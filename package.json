{
  "name": "openpool-webui",
  "private": true,
  "scripts": {
    "clean": "rm -Rf packages/*/dist/ packages/*/node_modules/",
    "build:app": "cd packages/app && npm install && npm run build",
    "build:site": "cd packages/site && npm install && npm run build",
    "build": "npm run clean && npm run build:app && npm run build:site"
  }
}
